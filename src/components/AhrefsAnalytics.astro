---
export interface Props {
  dataKey?: string;
}

const { dataKey } = Astro.props;
const isDev = import.meta.env.DEV;
---

{!isDev && dataKey && (
  <script async src="https://analytics.ahrefs.com/analytics.js" data-key={dataKey} />
)}
