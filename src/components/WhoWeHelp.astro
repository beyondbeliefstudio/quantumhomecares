---
// COMPONENTS
import Heading from "@/components/Heading.astro";

// HELPERS
import { Icon } from "astro-icon/components";

// DATA
import { whoWeHelp } from "@/data/homeData";
---

<section id="who" class="section" data-reveal>
  <div class="section-header who__header">
    <span class="eyebrow">Who We Help</span>
    <Heading tag="h2" size="xl" fontFamily="primary" weight="fw400" marginBottom="none">
      Families reach us at one of four moments.
    </Heading>
    <p class="lead who__lead">
      Requests for care come from all directions: the person who needs it, a spouse, a son or
      daughter, a friend, a social worker, a discharge planner. We welcome all of them.
    </p>
  </div>

  <div class="auto-grid who__grid">
    {
      whoWeHelp.map(item => (
        <article class="card who__card">
          <span class="icon-circle">
            <Icon name={item.icon} aria-hidden="true" />
          </span>
          <Heading tag="h3" size="xs" fontFamily="secondary" weight="fw600" marginBottom="none">
            {item.title}
          </Heading>
          <p>{item.copy}</p>
        </article>
      ))
    }
  </div>
</section>

<style lang="scss">
  .who {
    &__header {
      --w: 72ch;
      --mb: 3.25rem;
    }

    &__lead {
      --w: 62ch;
    }

    &__grid {
      --min: 17.5rem;
    }

    &__card {
      --gap: 0.75rem;
    }
  }
</style>
