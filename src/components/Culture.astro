---
// COMPONENTS
import Heading from "@/components/Heading.astro";

// DATA
import { culture } from "@/data/homeData";
---

<section class="band band--down" data-reveal>
  <div class="band__inner">
    <div class="section-header culture__header">
      <span class="eyebrow eyebrow--band">Our Culture</span>
      <Heading tag="h2" size="xl" fontFamily="primary" weight="fw400" marginBottom="none">
        Our operations and culture are anchored in the six Cs of Caregiving
      </Heading>
    </div>

    <div class="auto-grid culture__grid">
      {
        culture.map(item => (
          <div class="card culture__card">
            <Heading
              tag="span"
              size="s"
              fontFamily="secondary"
              weight="fw600"
              marginBottom="none"
              color="link"
            >
              {item.word}
            </Heading>
            <span class="culture__line">{item.copy}</span>
          </div>
        ))
      }
    </div>
  </div>
</section>

<style lang="scss">
  .culture {
    &__header {
      --w: 48ch;
      --mb: 3rem;
    }

    &__grid {
      --min: 21.25rem;
      --gap: 1.25rem;
    }

    &__card {
      --pad: 1.875rem 1.75rem;
      --gap: 0.375rem;
    }

    &__line {
      color: var(--qhc-ink-700);
      font-size: var(--qhc-size-label);
    }
  }
</style>
